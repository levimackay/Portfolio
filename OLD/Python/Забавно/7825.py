'''friends = []
friends.append({"name": "Chris", "state": "New Mexico", "age": 42})
friends.append({"name": "Zachariah", "state": "Utah", "age": 40})

print(friends[1].get("name")[::-1].capitalize())'''

from datetime import datetime as dt
current = dt.now()
print(current)
print(type(current))
print(dir(current))
